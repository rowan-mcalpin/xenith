package com.rowanmcalpin.xenith.opmode

/**
 * The default (empty) Controls class with no bound commands. Override the base [Controls] class to add your own custom
 * command bindings.
 */
class DefaultControls: Controls() {
    override fun bindCommands() {
        // There are no commands bound by default. Override the Controls class to customize your bound commands.
    }

}